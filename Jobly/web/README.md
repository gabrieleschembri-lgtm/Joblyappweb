# Deprecated

This Vite app is disabled. The web app for Jobly must be built from the Expo project.

Use:
- `cd JoblyApp && npx expo start --web` for local dev
- `cd JoblyApp && npx expo export -p web` for production export
